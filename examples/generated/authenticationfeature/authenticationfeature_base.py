# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .authenticationfeature_types import LoginLoginPost_Responses, LoginLoginPostParameters

if TYPE_CHECKING:
    from typing import TypeVar

    from sila2.server import SilaServer

    Server = TypeVar("Server", bound=SilaServer)


class AuthenticationFeatureBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """
        No description provided.
        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def LoginLoginPost(
        self, RequestParameters: LoginLoginPostParameters, *, metadata: MetadataDict
    ) -> LoginLoginPost_Responses:
        """
                Authenticate and receive JWT token

        Args:
            request: Login credentials
            service: Login service instance

        Returns:
            JWT access token


                :param RequestParameters: The parameters and payload of the request.

                :param metadata: The SiLA Client Metadata attached to the call

                :return:

                    - LoginResponseResponse: Response containing the LoginResponse.


        """
